# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More info on Python, GitHub Actions, and Azure App Service: https://aka.ms/python-webapps-actions

name: Build and deploy Streamlit app to Azure Web App - app-stakeholder360

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      # (Optional) Validate that dependencies install correctly — this doesn't deploy anything.
      - name: Validate dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Upload source files for deployment
      - name: Upload artifact for deployment jobs
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: .

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: python-app

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'app-stakeholder360'
          slot-name: 'Production'
<<<<<<< HEAD
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_ADEA600423E148DE8CCD4E9CD8A862C5 }}
=======
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_DD4279E488A44C829A0E0ADA8DE36F7D }}
>>>>>>> 4a6411802a92bac7f34f0869a7bc3837b23d290c
